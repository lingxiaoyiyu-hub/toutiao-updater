# =========================================================================
# XCbot · 今日头条文章采集大师与自媒体创作工作台 - 依赖清单
# =========================================================================

# 1. 基础 Web 与 API 服务框架
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.0.0
httpx>=0.27.0

# 2. 自动化采集与 HTML/DOM 逆向解析
playwright>=1.40.0
patchright>=1.40.0
beautifulsoup4>=4.12.0

# 3. 数据处理与多格式文档导出 (Excel / Word .docx / 美化终端)
openpyxl>=3.1.2
python-docx>=1.1.0
rich>=13.7.0

# 4. 客户端授权加密与底层安全
cryptography>=42.0.0

# 5. 桌面 GUI 客户端与可执行文件打包工具
pywebview>=5.0.0
pyinstaller>=6.5.0
pywin32>=306; sys_platform == 'win32'

